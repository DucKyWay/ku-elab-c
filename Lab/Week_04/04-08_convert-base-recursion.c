#include <stdio.h>
#include <stdlib.h>

int resursion_base(int x) {
    while(1) {
        
    }
    return 0;
}

int main() {

    char n_in[1000];
    int n = atoi( fgets(n_in, 1000, stdin) );


    return 0;
}