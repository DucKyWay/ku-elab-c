#include <stdio.h>

int main() {
    
    // ====================================================
    // char s[10] , c;
    // int i = 0, j = 034701, k;

    // while(j > 0) {
    //     s[i++] = j % 10;
    //     j /= 10;
    // }
    // s[k=i--] = 0;
    // while(i >= k/2) {
    //     c = s[i] + '0';
    //     s[i] = s[k-i-1] + '0';
    //     s[k-i-1] = c;
    //     i--;
    // }
    // printf("%s", s);

    // ====================================================

    // int n=1, a=2, b=3, z;

    // if(n > 0) if (a > b) z = a;
    // else z = b;
    // printf("%d\n", z);

    // ====================================================

    // int cnt = 1;
    // do {
    //     putchar('*');
    //     cnt *= 2;
    //     if(cnt >> 2) continue;
    //     putchar('*');
    // } while (cnt <= 16);

    // ====================================================

    // int arr[8] = {3, 4, 2, 3, 1, 4, 2};
    // int i, a=0, b=1, c=0, d=0;

    // for(i=0; i<8; i++) {
    //     a += arr[i];
    //     if(arr[i] % 2 == 0) {
    //         b *= arr[i];
    //         c++;
    //     } else d++;
    // }
    
    // ====================================================

    // int a, b;
    // scanf("%d %d", &a, &b);
    // if(a%2 == 0 && a >= 2*b) printf("T");
    // else printf("F");

    // ====================================================

    int i = 0;
    for(; i<8; i++);
    printf("%d\n", i);

    // ====================================================



    // ====================================================   

}